# ============================================================
# Studio Portfolio — Environment Variables
# ============================================================

# API server URL (build time — used for SSR/prerender)
# During development, the API runs at localhost:3001
# On the server, Nginx proxies /api/* to localhost:3001
API_URL=http://49.232.49.175:3001
