# ================================================================
# Supabase Docker 环境变量
# 复制为 .env 后修改
# ================================================================
# 生成 3 个随机密钥:
#   openssl rand -base64 32  (运行 3 次)
# ================================================================

POSTGRES_PASSWORD=change-me
JWT_SECRET=change-me
ANON_KEY=change-me
SERVICE_ROLE_KEY=change-me
SITE_URL=http://localhost:3000
ADMIN_EMAIL=admin@studio.com
SMTP_HOST=mail
SMTP_PORT=2500
SMTP_USER=
SMTP_PASS=

# 端口（已避开 investlearn 的 8080/5432/6379）
KONG_HTTP_PORT=8100
MAIL_UI_PORT=9100
